// Our project agentAve use Serverless application model
//what is a docker use for
//Docker use container.  Each container allow a developer 
//to package up an application with all of the parts it needs, such as libraries and other dependencies, 
//and ship it all out as one package
//This allow or eliminate customized settings that machine might have that could differ 
//from the machine used for writing and testing the code. 

//////////////////////////////////
// one way hashing or two hashing
